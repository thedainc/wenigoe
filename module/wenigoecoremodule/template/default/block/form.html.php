<!-- action value creates in automatic the link http://yoursite.com/wenigoecoremodule/form or http://yoursite.com/index.php?do=wenigoecoremodule/form IMPORTANT: change url "mylink" to url "link" in action. If I add this on article, phpFox convert this to http://phpfox.com/wenigoecoremodule/form --> <form method="post" action="{url mylink='wenigoecoremodule.form'}"> <!-- you can use the already existing class "table" of phpFox --> <div class="table"> <!-- you can use the already existing class "table_left" of phpFox for label --> <div class="table_left"> Select: </div> <!-- you can use the already exist class "table_right" of phpFox for field --> <div class="table_right"> <select name="val[select_id]" id="select"> <option value="">Make your choice:</option> {foreach from=$aSelects key=iKey item=aSelect} <!-- here we create a loop that renders the select with id and value --> <option value="{$aSelect.select_id}"{if $aArrayForEdit.select_id == $aSelect.select_id} selected{/if}> <!-- $aArrayForEdit is array if you edit the record. This is empty if you add a new one --> {$aSelect.select_value} <!-- $aSelect.select_value return the value of field, also $aSelect.select_id return the ID of row --> </option> {/foreach} <!-- end foreach --> </select> </div> <div class="clear"></div> <div class="table_left"> Textarea: </div> <div class="table_right"> <!-- $aArrayForEdit.textarea return the value of textare field if we edit row, otherwise this will be empty --> <textarea name="val[textarea]" id="textarea" rows="8" cols="50">{$aArrayForEdit.textarea}</textarea> </div> <div class="clear"></div> </div> <div class="table_clear"> {if $isEdit == 1} <!-- check if we edit a record or add a new one--> <input type="submit" value="Update" class="button" name="val[update]" /> <!-- if we edit a record, then we should add a hidden field with ID of the row for processing a correct row --> <input type="hidden" value="{$aArrayForEdit.mytable_id}" name="val[mytable_id]" /> {else} <!-- if we add a new row then it needs only a submit button. NOTE: the name of submit buttom changes, so we can know which operation we are doing on the controller --> <input type="submit" value="Add" class="button" name="val[add]" /> {/if} </div> </form>